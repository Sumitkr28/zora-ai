// /chat — the same ChatScreen as the web, minus persistence: @/lib/conversations
// resolves to this project's no-op stub, so nothing is written to Firestore.
export { default } from '@/app/chat/page';
